import SwiftUI

extension Font {
    /// Jeeves speaks clearly — slightly larger body text
    static let jeevesBody: Font = .body
    /// Monospaced for technical info (tool names, commands)
    static let jeevesMono: Font = .system(.footnote, design: .monospaced)
    /// Section headers
    static let jeevesHeadline: Font = .headline
    /// Small captions
    static let jeevesCaption: Font = .caption
}
